package main 

import (
	"fmt"
)
func main() {

var pr, p, s, n int

fmt.Print ("Insira dois numeros :")
fmt.Scan (p, s)
	if (p % 2== 0) {
		for n of 1 ate s {
		pr = (p + (n - 1) * 2)
		escreva (pr, " ")
		}
	}else {
	fmt.Print ("O PRIMEIRO NUMERO NAO E PAR")
	}

}